name: Deploy + Sync MongoDB

on:
  push:
    branches:
      - main
  schedule:
    - cron: "*/10 * * * *"

jobs:
  deploy:
    name: Deploy to InfinityFree
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Install lftp
        run: |
          sudo apt-get update
          sudo apt-get install -y lftp

      - name: Deploy via FTP
        env:
          FTP_USER: ${{ secrets.FTP_USER }}
          FTP_PASS: ${{ secrets.FTP_PASSWORD }}
          FTP_HOST: ${{ secrets.FTP_HOST }}
        run: |
          echo "🚀 Deploying to InfinityFree..."
          lftp -c "set ssl:verify-certificate no; open -u $FTP_USER,$FTP_PASS $FTP_HOST; mirror -R ./ /htdocs/ --ignore-time --parallel=2"
          echo "✅ Deployment done."

  sync_mongodb:
    name: Sync logs.json → MongoDB
    runs-on: ubuntu-latest
    needs: deploy

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Install runtime / dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y lftp python3 python3-pip
          python3 -m pip install --upgrade pip
          pip install pymongo dnspython

      - name: Download logs.json from InfinityFree (if not exist create empty)
        env:
          FTP_USER: ${{ secrets.FTP_USER }}
          FTP_PASS: ${{ secrets.FTP_PASSWORD }}
          FTP_HOST: ${{ secrets.FTP_HOST }}
        run: |
          echo "📥 Downloading /htdocs/logs.json..."
          rm -f logs.json
          if lftp -c "set ssl:verify-certificate no; open -u $FTP_USER,$FTP_PASS $FTP_HOST; get /htdocs/logs.json -o logs.json"; then
            echo "✅ logs.json downloaded."
          else
            echo "⚠ logs.json not found on server. Creating an empty logs.json with []"
            echo "[]" > logs.json
          fi
          ls -l logs.json

      - name: Sync with MongoDB (use secrets.MONGO_URI)
        env:
          MONGO_URI: ${{ secrets.MONGO_URI }}
          MONGO_DB: ${{ secrets.MONGO_DB }} # optional
          MONGO_COLLECTION: ${{ secrets.MONGO_COLLECTION }} # optional
        run: |
          echo "📡 Syncing logs to MongoDB..."
          python3 sync_logs_to_mongo.py || (echo "❌ Sync failed" && exit 1)

      - name: Upload stats_actions.csv
        env:
          FTP_USER: ${{ secrets.FTP_USER }}
          FTP_PASS: ${{ secrets.FTP_PASSWORD }}
          FTP_HOST: ${{ secrets.FTP_HOST }}
        run: |
          if [ -f stats_actions.csv ]; then
            echo "📤 Uploading stats_actions.csv..."
            lftp -c "set ssl:verify-certificate no; open -u $FTP_USER,$FTP_PASS $FTP_HOST; put stats_actions.csv -o /htdocs/stats_actions.csv"
            echo "✅ stats_actions.csv uploaded."
          else
            echo "⚠ stats_actions.csv not found"
          fi

      - name: Upload logs.csv
        env:
          FTP_USER: ${{ secrets.FTP_USER }}
          FTP_PASS: ${{ secrets.FTP_PASSWORD }}
          FTP_HOST: ${{ secrets.FTP_HOST }}
        run: |
          if [ -f logs.csv ]; then
            echo "📤 Uploading logs.csv..."
            lftp -c "set ssl:verify-certificate no; open -u $FTP_USER,$FTP_PASS $FTP_HOST; put logs.csv -o /htdocs/logs.csv"
            echo "✅ logs.csv uploaded."
          else
            echo "⚠ logs.csv not found"
          fi
