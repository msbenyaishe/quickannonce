name: Deploy to InfinityFree & Sync Logs

on:
  push:
    branches: [main]
  workflow_dispatch:  # Allow manual triggering
  schedule:
    - cron: "*/10 * * * *"  # Run every 10 minutes

jobs:
  deploy_and_sync:
    name: Deploy + Sync Logs
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      # --- Deployment to InfinityFree ---
      - name: Deploy to InfinityFree
        if: github.event_name == 'push'  # Only run on push events
        env:
          FTP_USER: ${{ secrets.FTP_USER }}
          FTP_PASS: ${{ secrets.FTP_PASSWORD }}
          FTP_HOST: ${{ secrets.FTP_HOST }}
        run: |
          echo "🚀 Starting deployment to InfinityFree..."
          # Add your deployment commands here
          # Example:
          # lftp -c "set ssl:verify-certificate no; 
          # open -u $FTP_USER,$FTP_PASS $FTP_HOST; 
          # mirror -R ./ /htdocs/ --ignore-time --parallel=10 --exclude-glob .git* --exclude .github/"

      # --- Log Synchronization ---
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'
      
      - name: Install Python dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pymongo python-dotenv python-dateutil
      
      - name: Install FTP client
        run: sudo apt-get update && sudo apt-get install -y lftp
      
      - name: Download logs.json
        env:
          FTP_USER: ${{ secrets.FTP_USER }}
          FTP_PASS: ${{ secrets.FTP_PASSWORD }}
          FTP_HOST: ${{ secrets.FTP_HOST }}
        run: |
          echo "📥 Downloading logs.json..."
          if ! lftp -c "set ssl:verify-certificate no; open -u $FTP_USER,$FTP_PASS $FTP_HOST; get /htdocs/logs.json -o logs.json"; then
            echo "[]" > logs.json
            echo "ℹ️ Created empty logs.json"
          fi
      
      - name: Run log synchronization
        env:
          MONGO_URI: ${{ secrets.MONGO_URI }}
          MONGO_DB: ${{ secrets.MONGO_DB || 'quickannonces' }}
          MONGO_COLLECTION: ${{ secrets.MONGO_COLLECTION || 'logs' }}
        run: |
          python sync_logs_to_mongo.py
          
      - name: Upload updated files
        env:
          FTP_USER: ${{ secrets.FTP_USER }}
          FTP_PASS: ${{ secrets.FTP_PASSWORD }}
          FTP_HOST: ${{ secrets.FTP_HOST }}
        run: |
          echo "📤 Uploading updated files..."
          lftp -c "
            set ssl:verify-certificate no;
            open -u $FTP_USER,$FTP_PASS $FTP_HOST;
            put -O /htdocs/ logs.csv;
            put -O /htdocs/ stats_actions.csv;
            put -O /htdocs/ logs.json;
          "
          echo "✅ Files uploaded successfully"